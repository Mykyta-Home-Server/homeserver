# ============================================================================
# Hello World Service Configuration
# ============================================================================
# Main website hosted at the root domain
# Container: hello-world (nginx:alpine)
# ============================================================================

# Main domain
mykyta-ryasny.dev {
    # Import Cloudflare Origin Certificate configuration
    import cf_tls

    # Reverse proxy to hello-world container on port 80
    reverse_proxy hello-world:80
}

# WWW subdomain - redirect to main domain
www.mykyta-ryasny.dev {
    # Import Cloudflare Origin Certificate configuration
    import cf_tls

    # Redirect to non-www version
    redir mykyta-ryasny.dev{uri}
}
